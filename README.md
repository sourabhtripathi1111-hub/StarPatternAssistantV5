# Star Pattern Assistant V5
Build-ready native Android source for GitHub Actions. V5 includes manual reading, CSV/Excel-compatible export, pattern engine, confidence/skip signal, overlay permission base, AI helper prompt. Real OCR/icon matching is the next module after APK build verification.
